/**
 * Rate limiting lives in @shared/auth (rateLimitGuard / checkRateLimit).
 * This stub remains so older imports resolve.
 */
export { rateLimitGuard as rateLimitMiddleware, authRateLimits } from "@shared/auth";
